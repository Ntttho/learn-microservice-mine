spring:
  application:
    name: order-service
#  cloud:
#    config:
#      uri: http://localhost:8761
#      fail-fast: true
  config:
    import: optional:configserver:http://localhost:8761

server:
  port: 8082