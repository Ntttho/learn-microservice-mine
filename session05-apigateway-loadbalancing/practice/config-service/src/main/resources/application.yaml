spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ntttho/config-repo
          default-label: main
          clone-on-start: true
#          search-paths: session03/config/config-repo
  profiles:
    active: dev

server:
  port: 8761


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false