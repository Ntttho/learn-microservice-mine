spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        # git:
        #   uri: https://github.com/Ntttho/learn-microservice-mine
        #   default-label: main
        #   clone-on-start: true
        #   search-paths: session03/config/config-repo
        native:
          search-locations: file:///D:/microservice-rikkei/session05-apigateway-loadbalancing/practice/config-file
  profiles:
    active: native # di tim vi tri file trong may local
    # active: dev # di tim git

server:
  port: 8888
