spring:
  application:
    name: order-service
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761
  instance:
    prefer-ip-address: true
server:
  port: 8080
